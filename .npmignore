# npm pack 语义：只影响通过 npm/pnpm 打包安装（如 `dsh plugin add github:...`）
# 的内容范围；不影响 git、Go 工具链、本地 file:/link: 安装（symlink 全量可见）。
# 保留：package.json / cordis.patch.yml / mcp/ / skills/ / README* / LICENSE*
bin/
dist/
logs/
tmp/
chat/
videos/
training/
examples/
*.log
.DS_Store
